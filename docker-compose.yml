version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    container_name: devkickstart-api
    environment:
      - ASPNETCORE_URLS=http://+:8080
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: devkickstart-redis
    ports:
      - "6379:6379"